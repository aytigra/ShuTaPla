<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Export options for `xcodebuild -exportArchive`, used by Scripts/release.sh.

  Developer ID distribution (outside the Mac App Store). `xcodebuild` re-signs the app and its
  nested Frameworks with the Developer ID Application identity and a secure timestamp, which is
  what makes the bundled libmpv closure notarizable. No provisioning profile is required — the
  app's only capabilities (sandbox, user-selected file access) don't need one for Developer ID.
-->
<plist version="1.0">
<dict>
	<key>method</key>
	<string>developer-id</string>
	<key>teamID</key>
	<string>JU443A4L25</string>
	<key>signingStyle</key>
	<string>automatic</string>
</dict>
</plist>
